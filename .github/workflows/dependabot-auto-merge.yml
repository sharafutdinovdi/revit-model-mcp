name: Dependabot auto-merge

on:
  pull_request_target:
    types: [opened, synchronize, reopened]

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  auto-merge:
    uses: sharafutdinovdi/.github/.github/workflows/dependabot-auto-merge.yml@main
    secrets: inherit
