name: Community

on:
  issues:
    types: [opened]
  pull_request_target:
    types: [opened, synchronize, reopened]
  schedule:
    - cron: '30 1 * * *'

permissions:
  contents: read
  issues: write
  pull-requests: write

jobs:
  community:
    uses: sharafutdinovdi/.github/.github/workflows/community.yml@main
    secrets: inherit
